# Data (tracked dirs, ignored contents)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
# Derived, small and non-sensitive -- committed because the deployed
# Streamlit dashboard can only read files that are in the repository.
!data/processed/scored_customers.csv

# DuckDB database files (rebuilt by src/run_sql.py)
data/*.duckdb
data/*.duckdb.wal

# Environments
venv/
.env

# Python
__pycache__/
*.pyc

# Notebooks
.ipynb_checkpoints

# macOS
.DS_Store
